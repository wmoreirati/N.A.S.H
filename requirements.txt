Flask==3.0.3
Flask-SQLAlchemy==3.1.1
python-dotenv==1.0.1
requests==2.34.2

# Provedores de IA — instale apenas o que for usar (ver AI_PROVIDER no .env).
# O Ollama (padrão) não precisa de biblioteca própria, só de `requests`.
openai==1.51.0
google-genai==2.22.0

# Dependência de desenvolvimento (opcional, só para rodar tests/)
pytest==8.3.3
