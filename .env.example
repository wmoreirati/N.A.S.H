# Copie este arquivo para ".env" e preencha com seus valores reais.
# NUNCA cometa o arquivo ".env" (com valores reais) em um repositório.

# ---------------------------------------------------------------------
# Provedor de IA
# ---------------------------------------------------------------------
# Qual modelo o N.A.S.H vai usar para conversar. Opções:
#
#   ollama      -> modelo local, gratuito, exige o Ollama instalado e rodando
#   openai      -> API da OpenAI          (exige OPENAI_API_KEY)
#   openrouter  -> catalogo do OpenRouter (exige OPENROUTER_API_KEY)
#   groq        -> Groq, gratuito sem cartao  (exige GROQ_API_KEY)
#   gemini      -> API do Google          (exige GEMINI_API_KEY)
#
# Atenção: o modelo local padrão é pequeno e erra contas. Para uso como
# professor de exatas, prefira "openrouter", "openai" ou "gemini".
AI_PROVIDER=ollama

# --- Opção 1: Ollama (local) ---
OLLAMA_BASE_URL=http://127.0.0.1:11434
OLLAMA_MODEL=qwen3:1.7b

# --- Opção 2: OpenAI ---
# Chave em: https://platform.openai.com/api-keys
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# --- Opção 3: OpenRouter (centenas de modelos com uma chave só) ---
# Chave em: https://openrouter.ai/keys
# Modelos e preços: https://openrouter.ai/models
#
# ATENÇÃO ao escolher o modelo: o N.A.S.H usa chamada de ferramentas para
# criar tarefas, projetos e memórias. Nem todo modelo do catálogo suporta
# isso — confira se o modelo aceita "tools" antes de apontar para ele.
OPENROUTER_API_KEY=
OPENROUTER_MODEL=openai/gpt-5-nano

# --- Opção 4: Groq (gratuito, sem cartao de credito) ---
# Chave em: https://console.groq.com/keys
# Modelos:  https://console.groq.com/docs/models
# Camada gratuita generosa, resposta muito rapida e com suporte a
# ferramentas. A lista de modelos muda com frequencia — confira o que esta
# ativo antes de fixar GROQ_MODEL.
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile

# --- Opção 5: Google Gemini ---
# Chave em: https://aistudio.google.com/apikey
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.6-flash

# ---------------------------------------------------------------------
# Banco de dados
# ---------------------------------------------------------------------
# Vazio  -> SQLite local em instance/nash.db (desenvolvimento)
# Preenchido -> Postgres (Supabase, em produção)
#
# ATENÇÃO: no Supabase, use a string do "Transaction pooler" (porta 6543),
# NÃO a conexão direta (5432). A direta é IPv6 e esgota o limite de conexões
# em ambiente serverless. A string do pooler tem esta cara:
#
#   postgresql://postgres.<ref-do-projeto>:<senha>@aws-0-<regiao>.pooler.supabase.com:6543/postgres
#
# O schema NÃO é criado sozinho em Postgres. Rode uma vez:
#   python scripts/init_db.py
DATABASE_URL=

# Força a criação automática do schema ao subir o servidor.
# Vazio = automático no SQLite, desligado no Postgres. Use 1 ou 0 para forçar.
DB_AUTO_CREATE=

# ---------------------------------------------------------------------
# Servidor
# ---------------------------------------------------------------------

# Porta do servidor Flask (padrão: 5000).
PORT=5000

# "1" para modo debug (recarrega automático + mais logs), "0" para produção.
# Mantenha 0 em qualquer servidor acessível pela internet.
FLASK_DEBUG=1

# Recomendado. Chave secreta do Flask (sessões/segurança). Se vazia, o servidor
# gera uma chave temporária a cada reinício (ok para uso local).
# Gere uma com: python -c "import secrets; print(secrets.token_hex(32))"
FLASK_SECRET_KEY=

# Quantas mensagens recentes (usuário+assistente) são enviadas como
# contexto ao modelo de IA a cada turno (padrão: 30).
MAX_HISTORY_MESSAGES=30
